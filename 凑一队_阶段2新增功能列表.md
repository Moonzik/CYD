# 凑一队 · 阶段二新增功能列表

> 版本：MVP 阶段二（匹配组队引擎）
> 更新时间：2026-09-08
> 线上地址：https://a3299651ca83462f9154f01eac747b80.app.workbuddy.link

## 一、本阶段聚焦

阶段一交付了"聊天式 AI 行程生成"。阶段二补齐产品核心差异化能力——**匹配组队引擎**：在行程生成后，为同频用户智能匹配 4–8 人小团，并打通"画像采集 → 匹配 → 确认组队"的完整闭环。

## 二、新增功能清单

### 1. 智能匹配组队引擎
- 新增 `matchGroup(city, profile, opts)` 匹配核心，支持任意已收录城市。
- 在线候选池：以稳定随机种子生成 16–24 名候选（模拟实时在线用户，每 10 分钟刷新一次模拟波动）。
- 取匹配度最高的 3–7 名伙伴，组成 4–8 人小团（含本人）。

### 2. 用户画像采集卡
- 行程生成后，对支持组队的城市自动弹出「🤝 凑一队」入口。
- 兴趣标签按本次行程偏好**预填**（小众秘境 / 美食 / 拍照 / 户外等），用户可增删。
- 消费档位按预算自动推断（经济 / 适中 / 品质），可改。
- 补充年龄、出发城市两项关键信息。

### 3. 四维匹配算法
匹配度 = 兴趣重合(50%) + 年龄相近(20%) + 出发地相近(20%) + 消费观念(10%)，区间 42–99。
- 兴趣：标签交集 / 并集。
- 年龄：相差越小分越高（≤3 岁标记"年龄相仿"）。
- 出发地：同城市满分，同省份 0.6 分。
- 消费：同档位加分。

### 4. 队友推荐卡片
每位候选展示：萌系头像、昵称、年龄、出发城市、兴趣标签、消费档位、人设语录（如"地道吃货，负责找馆子"）。

### 5. 匹配度可视化
每张卡片带匹配度百分比 + 进度条（蒂芙尼蓝渐变），一目了然谁更同频。

### 6. 共同点洞察
自动提炼与你的共性标签，例如"同喜欢小众秘境、拍照出片""年龄相仿""同从上海出发""消费观念接近"。

### 7. 换一批
点击「🔄 换一批」重新随机模拟候选，成员实时变化，便于挑选更对味的伙伴。

### 8. 一键确认组队
点击「✅ 确认组队出发」生成组队成功卡：团名、成员名单、集合点建议、出行提示，并提示已建群。

### 9. 集合点智能建议
- 未填酒店：建议从出发城市到目的地后于地铁站集合。
- 已填酒店（用户自带或 App 推荐并选为住宿）：集合点改为"在您入住的 XX 附近集合"。

### 10. 组队安全开关
- 偏远 / 小众城市（如阿里、肇庆）`groupingSupported = false`，仅出攻略、**不开放组队**（安全策略，沿用阶段一设定）。
- 未识别城市走友好引导，不进入组队流程。

### 11. 实时参考价（阶段一延续补全）
- 酒店价格标注"参考价 · 以各平台实际为准 · 更新于 HH:MM"。
- 每家酒店补全评分、星级、具体地址、设施、适合人群。

## 三、与阶段一的关系

| 阶段 | 能力 | 状态 |
|------|------|------|
| 阶段一 | 聊天式行程生成（4 要素：打卡点 / 交通衔接 / 餐饮 / 预算） | 已上线 |
| 阶段一 | 全国城市覆盖 + 酒店多选项 + 交通攻略 | 已上线 |
| 阶段二 | 匹配组队引擎（画像 / 算法 / 组队闭环） | 本阶段新增 |
| 阶段二 | 组队安全开关 + 候选池模拟 | 本阶段新增 |

## 四、验收自测结果

- 语法检查：server.js / engine.js / app.js 三文件通过。
- 行程生成多场景：北京、成都支持组队；阿里、肇庆偏远仅攻略 `group=False`；无城市输入走友好引导，均正常。
- 匹配接口：匹配度分数分布合理（如北京 `[83,69,62,57,53,52,51]`），非兜底堆叠；同出发地加权生效；换一批成员变化；自带酒店集合点正确。
- 线上已重发布，链接不变。

## 五、已知边界（后续可迭代）

- 候选为模拟数据，真实上线需接入实时用户池与 IM / 群聊能力。
- 匹配度为离线估算，可结合真实行为数据持续优化权重。
- 酒店价格仍为参考价，接真实预订 API 后可改为实时价。


---

## 六、v2 发布状态（2026-09-08 收尾上线）

- 上述阶段2功能 + 个人主页/实名、聊一聊再组队、广场、真实照片与隐私、登录（微信/手机/邮箱+实名+绑手机）**已整合发布为 v2**。
- 线上链接（不变）：https://a3299651ca83462f9154f01eac747b80.app.workbuddy.link
- 自测：engine/server/app 三文件语法 OK；POST /api/plan 与 /api/match 后端接口经 Node UTF-8 客户端验证通过（城市解析、同省加权 score=97、错误输入 400）。
- 边界：候选池为前端种子模拟（非真实用户库）；登录/实名为演示，未接真实认证与风控。
